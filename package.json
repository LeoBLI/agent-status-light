{
  "name": "agent-status-light",
  "version": "0.1.0",
  "description": "Local desktop status light for Codex approval states.",
  "main": "dist/main/index.js",
  "scripts": {
    "build": "node --max-old-space-size=256 ./node_modules/typescript/bin/tsc && npm run copy-renderer",
    "copy-renderer": "mkdir -p dist/renderer && cp src/renderer/index.html src/renderer/styles.css dist/renderer/",
    "prepare-electron": "node scripts/prepare-electron-dev.js",
    "dev": "npm run build && npm run prepare-electron && /private/tmp/agent-status-light-electron/Electron.app/Contents/MacOS/Electron .",
    "dev:server": "npm run build && node dist/main/server-only.js",
    "dev:stale-test": "STATUS_LIGHT_PORT=8788 STALE_TIMEOUT_MS=10000 npm run dev:server",
    "start": "npm run prepare-electron && /private/tmp/agent-status-light-electron/Electron.app/Contents/MacOS/Electron .",
    "status:approval": "curl -s -X POST http://localhost:8787/status -H 'Content-Type: application/json' -d '{\"agent\":\"codex\",\"state\":\"waiting_approval\",\"message\":\"Codex needs approval\"}'",
    "status:running": "curl -s -X POST http://localhost:8787/status -H 'Content-Type: application/json' -d '{\"agent\":\"codex\",\"state\":\"running\",\"message\":\"Codex is running\"}'",
    "status:idle": "curl -s -X POST http://localhost:8787/status -H 'Content-Type: application/json' -d '{\"agent\":\"codex\",\"state\":\"idle\",\"message\":\"Codex is idle\"}'",
    "diagnose": "node scripts/diagnose.js",
    "typecheck": "node --max-old-space-size=256 ./node_modules/typescript/bin/tsc --noEmit"
  },
  "keywords": [
    "codex",
    "electron",
    "status-light"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.0.0",
    "electron": "^35.7.5",
    "typescript": "^5.8.0"
  }
}
